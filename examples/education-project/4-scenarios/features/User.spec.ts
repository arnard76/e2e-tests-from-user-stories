import test from "@playwright/test";

test.describe("student's user", () => {
  test("sign-up", () => {});
  test("login", () => {});
  test("guest login", () => {});
  test("reset password", () => {});
  test("change password", () => {});
});
